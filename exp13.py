//Execute a script to check whether a number is divisible by 3
//For statements
# Initialize a list of numbers
numbers = [1, 2, 3, 4, 5]
# Iterate over the list of numbers using a for loop
for number in numbers:
  # Print the current number
  print(number)
